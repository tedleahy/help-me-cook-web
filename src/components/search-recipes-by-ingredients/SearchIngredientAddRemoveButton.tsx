import React from "react";
import { Button } from "react-bootstrap";

export default function SearchIngredientAddRemoveBtn() {
  return (
    <Button style={{ fontSize: "x-large" }} className="ml-1 py-0" variant="success">
      +
    </Button>
  );
}
